# MainFunctions

Small WPF .NET 8 project "MainFunctions".

Build & run

- Requires Visual Studio 2022 or newer with WPF workload.
- Target: .NET 8

Open the solution, build and run.

Notes

- This repository contains UI views and placeholder logic. Implement data access and navigation as needed.

## License

This project is licensed under the MIT License — see the `LICENSE` file for details.

## Pushing to GitHub

Example commands to add a remote and push:

```sh
# replace URL with your repository
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin main
```

Or use the included PowerShell script `scripts/git-commands.ps1` to automate initialization and pushing.
