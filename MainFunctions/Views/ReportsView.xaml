<UserControl x:Class="MainFunctions.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainFunctions.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F5F5" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Filters -->
            <RowDefinition Height="*"/>
            <!-- Content -->
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Margin="0,0,0,15">
            <TextBlock Text="Reports"
                   FontSize="26"
                   FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- FILTERS -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,15">
            <DatePicker x:Name="FromDate" Margin="0,0,10,0"/>
            <DatePicker x:Name="ToDate" Margin="0,0,10,0"/>
            <Button Content="Apply" Width="90"/>
        </StackPanel>

        <!-- MAIN CONTENT -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- SUMMARY CARDS -->
            <UniformGrid Rows="1" Columns="3" Margin="0,0,0,10">

                <Border Background="White" CornerRadius="10" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Total Revenue" FontWeight="Bold"/>
                        <TextBlock x:Name="RevenueValue" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Total Expenses" FontWeight="Bold"/>
                        <TextBlock x:Name="ExpenseValue" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Active Projects" FontWeight="Bold"/>
                        <TextBlock x:Name="ActiveProjectsValue" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

            </UniformGrid>

            <!-- TABLE -->
            <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
                <StackPanel>
                    <TextBlock Text="Report Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                    <DataGrid x:Name="ReportsGrid"
                          AutoGenerateColumns="True"
                          CanUserAddRows="False"
                          IsReadOnly="True"/>
                </StackPanel>
            </Border>

        </Grid>
        <Button Content="Export PDF" Margin="526,0,144,15" Grid.Row="1"/>
        <Button Content="Export Excel" Margin="646,0,24,15" Grid.Row="1"/>
    </Grid>

</UserControl>
