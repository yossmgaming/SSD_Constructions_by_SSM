<UserControl x:Class="MainFunctions.Views.MaterialPurchaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Supplier" VerticalAlignment="Center"/>
            <ComboBox x:Name="SupplierCombo" Width="220" Margin="8,0,0,0"/>
            <TextBlock Text="Or enter supplier" VerticalAlignment="Center" Margin="8,0,0,0"/>
            <TextBox x:Name="CustomSupplierBox" Width="220" Margin="8,0,0,0"/>
            <TextBlock Text="Project" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <ComboBox x:Name="ProjectCombo" Width="220" Margin="8,0,0,0"/>
            <TextBlock Text="Purchase Date" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <DatePicker x:Name="PurchaseDatePicker" Margin="8,0,0,0"/>
            <TextBlock Text="Due Date" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <DatePicker x:Name="DueDatePicker" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Input row to add materials -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,8">
            <TextBlock Text="Material" VerticalAlignment="Center"/>
            <ComboBox x:Name="MaterialSelect" Width="200" Margin="8,0,0,0"/>
            <TextBlock Text="Quantity" VerticalAlignment="Center" Margin="12,0,0,0"/>
            <TextBox x:Name="QuantityBox" Width="80" Margin="8,0,0,0"/>
            <TextBlock Text="Unit Rate" VerticalAlignment="Center" Margin="12,0,0,0"/>
            <TextBox x:Name="UnitRateBox" Width="100" Margin="8,0,0,0"/>
            <TextBlock Text="Notes" VerticalAlignment="Center" Margin="12,0,0,0"/>
            <TextBox x:Name="RowNotesBox" Width="200" Margin="8,0,0,0"/>
            <Button x:Name="AddMaterialButton" Content="Add" Margin="12,0,0,0"/>
            <Button x:Name="RemoveMaterialButton" Content="Remove Selected" Margin="8,0,0,0"/>
        </StackPanel>

        <DataGrid x:Name="MaterialsGrid" Grid.Row="2" AutoGenerateColumns="False" Margin="0,8,0,8" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Material" Binding="{Binding MaterialName}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Unit Rate" Binding="{Binding UnitRate, StringFormat=N2}" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Total:" VerticalAlignment="Center"/>
            <TextBlock x:Name="TotalAmountText" FontWeight="Bold" VerticalAlignment="Center" Margin="8,0,0,0"/>
            <Button x:Name="CreateHeaderButton" Content="Record Purchase" Margin="12,0,0,0" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>