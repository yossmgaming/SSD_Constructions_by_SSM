<UserControl x:Class="MainFunctions.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainFunctions.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Background="#F5F5F5" Margin="10">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="Auto"/>
                <!-- Stats -->
                <RowDefinition Height="Auto"/>
                <!-- Filters + Actions -->
                <RowDefinition Height="*"/>
                <!-- Table -->
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <TextBlock Text="Payments"
               FontSize="26"
               FontWeight="Bold"
               Margin="0,0,0,15"/>

            <!-- STATS -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">

                <Border Background="#2E7D32" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                    <TextBlock Text="Total Paid: LKR 0" Foreground="White" FontSize="16"/>
                </Border>

                <Border Background="#F9A825" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                    <TextBlock Text="Pending: LKR 0" Foreground="White" FontSize="16"/>
                </Border>

                <Border Background="#C62828" CornerRadius="10" Padding="15">
                    <TextBlock Text="Overdue: LKR 0" Foreground="White" FontSize="16"/>
                </Border>

            </StackPanel>

            <!-- FILTERS + ACTIONS -->
            <Grid Grid.Row="2" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="SearchBox"
                 Height="32"
                 Margin="0,0,10,0"
                 ToolTip="Search by project or worker..."/>

                <DatePicker x:Name="DateFilter"
                    Grid.Column="1"
                    Margin="0,0,10,0"/>

                <Button Grid.Column="2"
                Content="Add Payment"
                Width="120"
                Margin="0,0,10,0"
                Click="AddPayment_Click"/>

                <Button Grid.Column="3"
                Content="Refresh"
                Width="90"
                Click="Refresh_Click"/>
            </Grid>

            <!-- TABLE -->
            <DataGrid Grid.Row="3"
              x:Name="PaymentsGrid"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="True"
              SelectionMode="Single">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="140"/>
                    <DataGridTextColumn Header="Project" Binding="{Binding Project}" Width="*"/>
                    <DataGridTextColumn Header="Paid To" Binding="{Binding Recipient}" Width="160"/>
                    <DataGridTextColumn Header="Amount (LKR)" Binding="{Binding Amount}" Width="140"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>


    </Grid>
</UserControl>
