<UserControl x:Class="MainFunctions.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F5F5">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Margin="0,0,0,15">
            <TextBlock Text="Payments &amp; Accounts"
                       FontSize="26"
                       FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <ComboBox x:Name="ProjectSelector" Width="200" Margin="0,0,8,0" IsEditable="True" IsTextSearchEnabled="True" ToolTip="Select Project"/>
                <DatePicker x:Name="MonthPicker" Width="130" DisplayDateStart="2000-01-01" ToolTip="Pick any date in month"/>
                <Button x:Name="GeneratePayrollButton" Content="Generate Payroll (Month)" Width="180" Height="32" Margin="8,0,0,0" Click="GeneratePayrollButton_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- SUMMARY -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="4" Margin="0,0,0,15">
            <Border Background="#1565C0" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Total Receivable" Foreground="White"/>
                    <TextBlock x:Name="TotalReceivableText" Text="LKR 0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
            <Border Background="#2E7D32" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Total Payable" Foreground="White"/>
                    <TextBlock x:Name="TotalPayableText" Text="LKR 0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
            <Border Background="#C62828" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Overdue Amount" Foreground="White"/>
                    <TextBlock x:Name="OverdueAmountText" Text="LKR 0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
            <Border Background="#F9A825" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Net Position" Foreground="White"/>
                    <TextBlock x:Name="NetPositionText" Text="LKR 0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- FILTERS -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Search" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox x:Name="SearchBox" Height="30" ToolTip="Search worker, project..."/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="From" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <DatePicker x:Name="FromDateFilter" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="8,0,0,0">
                    <TextBlock Text="To" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <DatePicker x:Name="ToDateFilter" Height="30"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="8,0,0,0">
                    <TextBlock Text="Header Type" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <ComboBox x:Name="TypeFilter" Height="30">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="WorkerPayroll"/>
                        <ComboBoxItem Content="ClientInvoice"/>
                        <ComboBoxItem Content="MaterialPurchase"/>
                        <ComboBoxItem Content="ProjectExpense"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="8,0,0,0">
                    <TextBlock Text="Status" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <ComboBox x:Name="StatusFilter" Height="30">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Paid"/>
                        <ComboBoxItem Content="Partial"/>
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Overdue"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button x:Name="RefreshButton" Content="Refresh" Width="90" Height="32" Margin="0,0,8,0" Click="Refresh_Click"/>
                    <Button x:Name="ApplyButton" Content="Apply" Width="90" Height="32" Click="Refresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="420"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Tabs with Headers -->
            <TabControl Grid.Column="0">
                <TabItem Header="All">
                    <DataGrid x:Name="HeadersGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Width="120" Binding="{Binding Type}"/>
                            <DataGridTextColumn Header="Project" Width="160" Binding="{Binding ProjectName}"/>
                            <DataGridTextColumn Header="Entity" Width="160" Binding="{Binding EntityName}"/>
                            <DataGridTextColumn Header="Period" Width="160" Binding="{Binding Period}"/>
                            <DataGridTextColumn Header="Total" Width="120" Binding="{Binding TotalLines, StringFormat=LKR {0:N2}}"/>
                            <DataGridTextColumn Header="Settled" Width="120" Binding="{Binding TotalSettlements, StringFormat=LKR {0:N2}}"/>
                            <DataGridTextColumn Header="Balance" Width="120" Binding="{Binding Balance, StringFormat=LKR {0:N2}}"/>
                            <DataGridTextColumn Header="Status" Width="120" Binding="{Binding Status}"/>
                            <DataGridTextColumn Header="Due" Width="120" Binding="{Binding DueDate}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="Overdue">
                    <DataGrid x:Name="OverdueGrid" AutoGenerateColumns="False" IsReadOnly="True"/>
                </TabItem>
                <TabItem Header="Receivables">
                    <DataGrid x:Name="ReceivablesGrid" AutoGenerateColumns="False" IsReadOnly="True"/>
                </TabItem>
                <TabItem Header="Payables">
                    <DataGrid x:Name="PayablesGrid" AutoGenerateColumns="False" IsReadOnly="True"/>
                </TabItem>
            </TabControl>

            <!-- Right: Header details, Create Header, and Settlement -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="12" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="Header Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

                    <!-- Advanced Mode warning banner -->
                    <Border x:Name="AdvancedBanner" Background="#FFF3CD" BorderBrush="#FFECB5" BorderThickness="1" CornerRadius="6" Padding="8" Visibility="Collapsed" Margin="0,0,0,8">
                        <TextBlock Text="You are in Advanced Mode. Manual entries bypass automation — use only for corrections." FontSize="12"/>
                    </Border>
                    <CheckBox x:Name="AdvancedModeToggle" Content="Advanced Mode" Margin="0,0,0,8"/>

                    <TextBlock Text="Type"/>
                    <ComboBox x:Name="PaymentType" Height="28" Margin="0,0,0,8">
                        <ComboBoxItem Content="WorkerPayroll"/>
                        <ComboBoxItem Content="ClientInvoice"/>
                        <ComboBoxItem Content="MaterialPurchase"/>
                        <ComboBoxItem Content="ProjectExpense"/>
                    </ComboBox>

                    <TextBlock Text="Project"/>
                    <ComboBox x:Name="PaymentProjectCombo" Height="28" Margin="0,0,0,8" IsEditable="True" IsTextSearchEnabled="True"
                              DisplayMemberPath="Name" SelectedValuePath="Id"/>

                    <TextBlock Text="Entity"/>
                    <ComboBox x:Name="PaymentEntityCombo" Height="28" Margin="0,0,0,8" IsEditable="True" IsTextSearchEnabled="True"/>

                    <TextBlock Text="Period"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <DatePicker x:Name="PeriodStartPicker" Width="120" Margin="0,0,8,0"/>
                        <DatePicker x:Name="PeriodEndPicker" Width="120"/>
                    </StackPanel>

                    <TextBlock Text="Notes"/>
                    <TextBox x:Name="PaymentNotes" Height="28" Margin="0,0,0,8"/>

                    <TextBlock Text="Due Date"/>
                    <DatePicker x:Name="PaymentDueDate" Height="28" Margin="0,0,0,12"/>

                    <!-- Totals and Status (read-only) -->
                    <TextBlock Text="Totals"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Total:" Width="60"/>
                        <TextBox x:Name="PaymentTotal" Height="28" Width="100" IsReadOnly="True"/>
                        <TextBlock Text="Settled:" Margin="12,0,0,0" Width="70"/>
                        <TextBox x:Name="PaymentPaid" Height="28" Width="100" IsReadOnly="True"/>
                        <TextBlock Text="Balance:" Margin="12,0,0,0" Width="70"/>
                        <TextBox x:Name="PaymentBalance" Height="28" Width="100" IsReadOnly="True"/>
                    </StackPanel>

                    <TextBlock Text="Status"/>
                    <ComboBox x:Name="PaymentStatus" Height="28" Margin="0,0,0,12" IsEnabled="False">
                        <ComboBoxItem Content="Paid"/>
                        <ComboBoxItem Content="Partial"/>
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Overdue"/>
                    </ComboBox>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Amount" Width="60"/>
                        <TextBox x:Name="CreateHeaderAmount" Height="28" Width="120"/>
                        <Button x:Name="CreateHeaderButton" Content="Create Payment" Width="140" Height="28" Margin="8,0,0,0"/>
                    </StackPanel>

                    <Separator Margin="0,8,0,8"/>
                    <TextBlock Text="Add Settlement" FontSize="14" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <DatePicker x:Name="SettlementDate" Width="120"/>
                        <TextBox x:Name="SettlementAmount" Width="100" Margin="8,0,0,0"/>
                        <ComboBox x:Name="SettlementMethod" Width="120" Margin="8,0,0,0">
                            <ComboBoxItem Content="Cash"/>
                            <ComboBoxItem Content="Bank"/>
                            <ComboBoxItem Content="Cheque"/>
                        </ComboBox>
                        <Button x:Name="AddSettlementButton" Content="Add" Width="80" Margin="8,0,0,0" Click="AddSettlementButton_Click"/>
                    </StackPanel>

                    <!-- Settlements list with reversal -->
                    <TextBlock Text="Header Settlements" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,4"/>
                    <DataGrid x:Name="HeaderSettlementsGrid" AutoGenerateColumns="False" IsReadOnly="True" Height="160">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=yyyy-MM-dd}" Width="100"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}" Width="100"/>
                            <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="100"/>
                            <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="100"/>
                            <DataGridCheckBoxColumn Header="Reversal" Binding="{Binding IsReversal}" Width="80"/>
                            <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                        <Button x:Name="ReverseSettlementButton" Content="Reverse Selected" Width="140"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button x:Name="ExportPaymentsButton" Content="Export CSV" Width="90" Height="32" Click="ExportPaymentsButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
