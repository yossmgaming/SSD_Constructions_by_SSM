<UserControl x:Class="MainFunctions.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Background="#F5F5F5" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: navigation, worker search & list -->
        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <Button x:Name="BackButton" Content="Workers" Height="32" Width="120" HorizontalAlignment="Left" Margin="0,0,0,8" Click="BackButton_Click"/>
            <TextBlock Text="Workers" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBox x:Name="SearchWorkerBox" Height="32" ToolTip="Search worker by name..." TextChanged="SearchWorkerBox_TextChanged"/>
            <ListBox x:Name="WorkersListBox" Margin="0,8,0,0" DisplayMemberPath="FullName" SelectionChanged="WorkersListBox_SelectionChanged"/>
        </StackPanel>

        <!-- RIGHT: project filters, calendar and controls -->
        <StackPanel Grid.Column="1">
            <!-- Top controls: project + month navigation; legend on the right -->
            <DockPanel Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="Project" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="ProjectFilter" Width="220" Height="32" SelectionChanged="ProjectFilter_SelectionChanged"/>

                    <!-- Month navigation and label -->
                    <Button x:Name="PrevMonthButton" Content="◀" Width="34" Height="32" Margin="12,0,0,0" Click="PrevMonthButton_Click"/>
                    <TextBlock x:Name="MonthLabel" Text="Month" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="8,0"/>
                    <Button x:Name="NextMonthButton" Content="▶" Width="34" Height="32" Click="NextMonthButton_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Border Background="LightGreen" Width="16" Height="16" Margin="0,0,6,0"/>
                    <TextBlock Text="Present" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Border Background="Tomato" Width="16" Height="16" Margin="0,0,6,0"/>
                    <TextBlock Text="Absent" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>

            <!-- Export and summary row -->
            <DockPanel Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="ExportMonthlyButton" Content="Export Month" Height="32" Width="130" Click="ExportMonthlyButton_Click"/>
                    <Button x:Name="ExportCsvButton" Content="Export CSV" Height="32" Width="120" Margin="8,0,0,0" Click="ExportCsvButton_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="SummaryModeText" Margin="0,0,12,0" FontStyle="Italic"/>
                    <TextBlock Text="Assigned:" Margin="0,0,6,0"/>
                    <TextBlock x:Name="AssignedCountText" FontWeight="Bold"/>
                    <TextBlock x:Name="PresentLabelText" Text="  |  Present (days):" Margin="8,0,6,0"/>
                    <TextBlock x:Name="PresentCountText" FontWeight="Bold"/>
                    <TextBlock x:Name="AbsentLabelText" Text="  |  Absent (days):" Margin="8,0,6,0"/>
                    <TextBlock x:Name="AbsentCountText" FontWeight="Bold"/>
                </StackPanel>
            </DockPanel>

            <!-- hint when All Projects selected -->
            <TextBlock x:Name="ProjectHintText" Foreground="Gray" Margin="0,0,0,8" Visibility="Collapsed"
                       Text="Select a project to view attendance"/>

            <!-- calendar grid -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <UniformGrid x:Name="CalendarGrid" Columns="7" HorizontalAlignment="Stretch"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>