<UserControl x:Class="MainFunctions.Views.ProjectExpenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Project" VerticalAlignment="Center"/>
            <ComboBox x:Name="ProjectCombo" Width="220" Margin="8,0,0,0"/>
            <TextBlock Text="Category" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <ComboBox x:Name="CategoryCombo" Width="200" Margin="8,0,0,0"/>
            <TextBlock Text="Date" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <DatePicker x:Name="DatePicker" Margin="8,0,0,0"/>
            <TextBlock Text="Due Date" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <DatePicker x:Name="DueDatePicker" Margin="8,0,0,0"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Description"/>
                <TextBox x:Name="DescriptionBox" Height="60" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                <TextBlock Text="Amount"/>
                <TextBox x:Name="AmountBox" Width="140"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CreateHeaderButton" Content="Record Expense"/>
        </StackPanel>
    </Grid>
</UserControl>