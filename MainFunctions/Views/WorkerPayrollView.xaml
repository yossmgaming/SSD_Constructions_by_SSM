<UserControl x:Class="MainFunctions.Views.WorkerPayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Project" VerticalAlignment="Center"/>
            <ComboBox x:Name="ProjectCombo" Width="220" Margin="8,0,0,0"/>
            <TextBlock Text="Period" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <DatePicker x:Name="PeriodStartPicker" Margin="8,0,0,0"/>
            <TextBlock Text="to" VerticalAlignment="Center" Margin="8,0,0,0"/>
            <DatePicker x:Name="PeriodEndPicker" Margin="8,0,0,0"/>
            <TextBlock Text="Worker" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <ComboBox x:Name="WorkerCombo" Width="220" Margin="8,0,0,0"/>
        </StackPanel>
        <DataGrid x:Name="AttendanceGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,8,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=yyyy-MM-dd}"/>
                <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked}"/>
                <DataGridTextColumn Header="Half Day" Binding="{Binding IsHalfDay}"/>
                <DataGridTextColumn Header="Daily Amount" Binding="{Binding DailyAmount, StringFormat=N2}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Total:" VerticalAlignment="Center"/>
            <TextBlock x:Name="TotalAmountText" FontWeight="Bold" VerticalAlignment="Center" Margin="8,0,0,0"/>
            <Button x:Name="CreateHeaderButton" Content="Create Payroll" Margin="12,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock Text="Payment Reference" VerticalAlignment="Center"/>
            <TextBox x:Name="PayrollReferenceBox" Width="300" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>